# Calendar

ตารางเดือน — **ฐานเดียว** ของ `DatePicker` และ `DateNavigator`

| | |
|---|---|
| โค้ด | `packages/react/src/ui/Calendar.tsx` |
| Storybook | `UI/Calendar` |
| เทส | `Calendar.test.tsx` — 24 เคส |
| ใครใช้ | `form/DatePicker.tsx` · `ui/DateNavigator.tsx` |
| Figma | ไฟล์ **Mediact Design System** → หน้า `Navigation` |
| สถานะ sync | ✅ ตรงกัน — 2026-08-10 |

---

## ยึดตามของจริงในหน้า productivity

ต้นแบบคือ `PickerCalendar` ของ `mediact-web-backoffice`
(`src/components-v2/shared/pickerCalendar.tsx`) ซึ่งเป็นปฏิทินตัวเดียวที่จอ
**บันทึกยอดเวร · ตัวกรองช่วงวัน · ตัวเลือกวัน+เวรของ productivity** เปิดเหมือนกันหมด

ทุกค่าในไฟล์นั้นเป็น `sx` ที่เขียนตัวเลขตรง ๆ (ไม่มี `theme.spacing` ที่คำนวณ)
⇒ **อ่านซอร์สคือการวัด** — ต่างจากกรณี Mediwork ทั่วไปที่ต้องเปิดเบราว์เซอร์
วัดซ้ำใน Storybook แล้ว **ตรงครบ 13/13**:

| จุด | ค่า |
|---|---|
| กล่อง | กว้าง **340** · pad 16 · ล่าง 0 |
| ปุ่มลูกศรหัวปฏิทิน | 28×28 · มุม 8 · ไอคอน **18** |
| ชื่อเดือน | **15px / 700** · มุม 8 · pad 8/4 |
| หัวคอลัมน์วัน | 12px · จาง |
| ช่องวัน | สูง **40** |
| วงกลมวัน | **34×34** · 14px |
| ตาราง 12 เดือน | 3 คอลัมน์ · ช่องสูง **44** · มุม **10** · เว้น 8 |
| จำนวนช่อง | **42 เสมอ** (6 แถวคงที่) |

---

## ทำไมเขียนเอง ไม่ใช้ `react-day-picker` / MUI `DateCalendar`

เหตุผลเดียวกับที่แอปเขียนเอง — และเป็นเหตุผลที่ยังจริงอยู่:

1. **ไม่มีตัวแปลง พ.ศ.** ทั้งสองตัว · ของเราใช้ `Intl` ตรง ๆ ซึ่ง `th-TH`
   ให้ปีพุทธมาเอง ไม่ต้องมี adapter แยก
2. **แถบเชื่อมช่วงวัน** เป็นสิ่งที่ `DateCalendar` ไม่มีแนวคิดรองรับ
3. พอเขียนเพื่อช่วงแล้ว **วันเดี่ยวก็ใช้ตัวเดียวกัน** — ไม่งั้นแอปจะมีปฏิทินสองแบบ
   ที่นับปีคนละอย่าง ซึ่งแย่กว่าเลือกแบบใดแบบหนึ่งไปเลย (แอปเคยเป็นแบบนั้นจริง:
   ตัวกรองขึ้น 69 ส่วนตารางข้าง ๆ ขึ้น 2026)

ผลพลอยได้: ถอด `react-day-picker` ออกจาก dependency ได้ พร้อม `style.css`
ที่มันบังคับ import ซึ่งเป็นแหล่ง drift กับชั้น token

⚠️ โครง DOM ยังเป็น `<table role="grid">` + `<td role="gridcell" data-day="YYYY-MM-DD">`
ตามสัญญาเดิมของ `react-day-picker` **โดยตั้งใจ** — เทสของ `DatePicker` ที่มีอยู่
ผ่านต่อได้โดยไม่ต้องแก้สักบรรทัด และเป็น ARIA ที่ถูกต้องอยู่แล้ว

---

## เรื่องที่ต้องรู้

### 🔴 `toISOString()` ทำคีย์วันที่ไม่ได้

มันแปลงเป็น UTC ก่อน ⇒ โซนไทย (UTC+7) ได้วันที่ **ย้อนไป 1 วัน** ทุกครั้งที่เวลา
ต่ำกว่า 07:00 — บั๊กที่ไม่มีใครเห็นจนกว่าจะมีคนทดสอบตอนเช้า
`dayKey()` จึงประกอบจาก `getFullYear/getMonth/getDate` ตรง ๆ และมีเทสล็อกไว้

### วันของเดือนข้างเคียง — วาด แต่กดไม่ได้

ถ้าไม่วาด ตารางจะกระโดดระหว่าง 5 กับ 6 แถวตอนเปลี่ยนเดือน
ถ้าให้กดได้ การกด "26" ตอนดูเดือน พ.ค. จะเด้งไปเมษายนและเลื่อนวันที่เลือกออกนอกจอ
ซึ่งผู้ใช้จะอ่านว่าปฏิทินทำอย่างอื่น

### มุมมอง 12 เดือน — ลูกศรเลื่อนทีละปี

ชื่อเดือนคือทางเข้า (ที่เดียวกับที่ปฏิทินทุกตัวใส่ไว้) · ในมุมมองนั้นลูกศรเปลี่ยน
หน่วยเป็นปี เพราะนั่นคือหน่วยที่ผู้ใช้กำลังมองอยู่ — ถ้าไม่มีมุมมองนี้ การย้อนกลับ
ไปหนึ่งปีคือกดลูกศร 12 ครั้ง และปฏิทินนี้มาแทนช่องที่เคยพิมพ์วันที่ได้

`selectMonth` = เลือกเดือนแล้วจบเลย ใช้กับ `DateNavigator unit="month"`
ซึ่งหน่วยของมันคือเดือน การบังคับให้กดวันต่ออีกทีคือขั้นตอนที่ไม่มีความหมาย

### คีย์บอร์ด

ปฏิทินทั้งเดือนมีจุด tab **เดียว** ไม่ใช่ 42 จุด · ลูกศรเลื่อนวัน/สัปดาห์
`Home`/`End` ไปต้น/ท้ายสัปดาห์ · `PageUp`/`PageDown` เลื่อนเดือน
เดินข้ามขอบเดือนแล้วเปลี่ยนเดือนให้เอง

ทุกวันมี `aria-label` เป็นวันที่เต็ม ("วันศุกร์ที่ 15 พฤษภาคม พ.ศ. 2569")
ไม่ใช่แค่ตัวเลข — ตัวเลขลอย ๆ ในตารางไม่บอกอะไรกับโปรแกรมอ่านหน้าจอ

### ทุกปุ่มเป็น `type="button"`

ปฏิทินอยู่ในฟอร์มเสมอ · ปุ่มที่ลืมตั้ง `type` จะ submit ทั้งฟอร์มตอนกดเลือกวัน
เป็นบั๊กที่หาสาเหตุยากมาก — มีเทสไล่ทุกปุ่มในไฟล์

---

## สีที่ใช้ และเรื่องที่ยังไม่จบ

| จุด | token | ค่าจริงในแอป |
|---|---|---|
| วันที่เลือก | `bg-brand` + `text-brand-foreground` | `#019688` |
| แถบช่วงวัน | `bg-brand-subtle` | `#E9F7F5` |
| ปุ่มลูกศร / hover | `bg-overlay-hover` · `bg-overlay-press` | `#F1F5F9` |
| ตัวเลขวัน | `text-text-black` | `#0F172A` |
| หัวคอลัมน์ | `text-text-tertiary` | `#94A3B8` |
| วันที่กดไม่ได้ | `text-text-disabled` | `#CBD5E1` |

🔴 **คอนทราสต์ของวันที่เลือกยังตกเกณฑ์ ทั้งของแอปและของ DS** — ตัวเลขในวงกลม
เป็นข้อความ 14px จึงต้องการ 4.5:1

| | สี | คอนทราสต์กับขาว |
|---|---|---|
| แอป (ค่าที่เลือกเอง) | `#019688` | **3.67:1** ❌ |
| DS · Portal | `#43596e` | 7.26:1 ✅ |
| DS · MediHR | `#0611ac` | 12.47:1 ✅ |
| DS · Medimatch | `#0395d8` | **3.34:1** ❌ |
| DS · Mediwork | `#26d1b3` | **1.93:1** ❌ |

น่าสังเกตว่าแอป **ไม่ได้ใช้สีแบรนด์ของตัวเอง** (`#26d1b3`) ตรงนี้ — เลือกเขียวเข้มกว่า
เองเพราะมิ้นต์อ่อนเกินไป แต่ก็ยังไม่ผ่านอยู่ดี

DS ใช้ `bg-brand` ให้เปลี่ยนตามแอปเหมือน component อื่นทุกตัว **ไม่แก้เป็นราย
component** เพราะนี่คือก้อนเดียวกับข้อ 6 ใน `CLAUDE.md` (ขาวบนแบรนด์ตกที่ 2 ใน 4 แอป
รวม 13+ จุดเรียก) ทางแก้คือ override `--color-brand-foreground` ต่อธีม หรือทำ
`--color-brand` ให้เข้มขึ้น — **เป็นการตัดสินใจของทีมออกแบบ ยังไม่ทำ**

---

## ใครใช้ปฏิทินนี้บ้าง

### `DatePicker`

หนึ่งคลิกคือทั้งการตัดสินใจ ⇒ **commit แล้วปิดทันที ไม่มีปุ่ม OK**
(ปุ่มยืนยันเป็นเรื่องของช่วงวัน/วัน+เวร ซึ่งเลือกครั้งเดียวยังไม่ครบคู่)

⚠️ **breaking** — ถอด prop ที่ผูกกับ `react-day-picker` ออก:
`captionLayout` · `fromYear` · `toYear` · แทนด้วย `calendarLocale` ·
`weekStartsOn` · `calendarLabels` · ตรวจแล้วไม่มีแอปไหน import `DatePicker`
จาก `@mediact/react` เลยสักที่ (ครบ 6 checkout)

### `DateNavigator`

ดู [`DateNavigator.md`](./DateNavigator.md)

---

## บันทึกการตัดสินใจ

| วันที่ | เปลี่ยนอะไร | ต้นทุนที่ยอมรับ |
|---|---|---|
| 2026-08-10 | สร้าง `Calendar` เป็นฐาน · ถอด `react-day-picker` | ต้องดูแลคีย์บอร์ด/ARIA เอง — แลกกับปฏิทินเดียวทั้ง DS และรองรับ พ.ศ. + ช่วงวัน |
| 2026-08-10 | คงโครง `role="grid"` + `data-day` ของ `react-day-picker` | ผูกกับสัญญาของไลบรารีที่ถอดออกไปแล้ว — แต่เป็น ARIA ที่ถูก และเทสเดิมผ่านต่อ |
| 2026-08-10 | ใช้ `bg-overlay-hover`/`press` แทน `#F1F5F9` | สีต่างจากแอปเล็กน้อย แลกกับไม่มีสีดิบเลยสักตัวในไฟล์ |

---

## ยังไม่ได้ทำ

- ไม่มี `isLoading` (โครงร่างตอนโหลด) — ปฏิทินไม่ต้องรอข้อมูล
- ยังไม่มี `DateRangePicker` เป็น component ของตัวเอง ทั้งที่ `Calendar` รองรับช่วง
  ครบแล้ว (`rangeEnd` · `hoverEnd`) — ตอนนี้ผู้เรียกต้องประกอบเอง
- เวลา (`TimePicker`) ยังเป็นคนละตัว ไม่ได้รวมร่างกัน
